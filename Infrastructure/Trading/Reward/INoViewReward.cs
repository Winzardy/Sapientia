namespace Trading
{
	/// <summary>
	/// Marker for a <see cref="TradeReward"/> that must not be shown in reward UI (e.g. reward lists/widgets).
	/// </summary>
	public interface INoViewReward
	{
	}
}
